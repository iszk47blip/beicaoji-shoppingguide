.chat-container {
  display: flex;
  flex-direction: column;
  height: 100vh;
  background: #f0f2f0;
}

.chat-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20rpx 28rpx;
  background: #5B8C5A;
  color: #fff;
}

.header-title {
  font-size: 30rpx;
  font-weight: 600;
}

.header-orders {
  font-size: 24rpx;
  opacity: 0;
  transition: opacity 0.2s;
}

.header-orders.visible {
  opacity: 1;
}

.message-list {
  flex: 1;
  padding: 20rpx 24rpx;
}

.message-list.has-cart {
  padding-bottom: 100rpx;
}

.msg-row {
  display: flex;
  margin-bottom: 20rpx;
  animation: msgIn 0.25s ease-out;
}

.msg-row.user {
  justify-content: flex-end;
}

.msg-row.bot {
  justify-content: flex-start;
}

.msg-row.system {
  justify-content: center;
}

.msg-block {
  max-width: 82%;
  display: flex;
  flex-direction: column;
}

.msg-row.user .msg-block {
  max-width: 75%;
}

.bubble {
  padding: 20rpx 28rpx;
  border-radius: 18rpx;
  font-size: 28rpx;
  line-height: 1.55;
  word-break: break-word;
}

.msg-row.user .bubble {
  background-color: #5B8C5A;
  color: #fff;
  border-bottom-right-radius: 6rpx;
}

.msg-row.bot .bubble {
  background-color: #fff;
  color: #333;
  border-bottom-left-radius: 6rpx;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.06);
}

.msg-row.system .bubble {
  background-color: #e8f5e9;
  color: #5B8C5A;
  font-size: 24rpx;
  text-align: center;
}

/* Typing dots */
.typing {
  display: flex;
  align-items: center;
  gap: 8rpx;
  padding: 28rpx 36rpx;
}

.dot {
  width: 12rpx;
  height: 12rpx;
  border-radius: 50%;
  background: #999;
  animation: bounce 1.2s infinite;
}

.dot:nth-child(2) { animation-delay: 0.2s; }
.dot:nth-child(3) { animation-delay: 0.4s; }

@keyframes bounce {
  0%, 60%, 100% { transform: translateY(0); }
  30% { transform: translateY(-12rpx); }
}

@keyframes msgIn {
  from { opacity: 0; transform: translateY(8rpx); }
  to   { opacity: 1; transform: translateY(0); }
}

/* Quick replies */
.quick-replies {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  padding: 12rpx 24rpx;
  background: #f0f2f0;
}

.quick-btn {
  padding: 14rpx 28rpx;
  background: #fff;
  color: #5B8C5A;
  border: 1rpx solid #5B8C5A;
  border-radius: 36rpx;
  font-size: 26rpx;
}

.quick-btn:active {
  background: #5B8C5A;
  color: #fff;
}

/* Input bar */
.input-bar {
  display: flex;
  align-items: center;
  padding: 16rpx 20rpx;
  background: #fff;
  border-top: 1rpx solid #eee;
}

.input {
  flex: 1;
  height: 72rpx;
  padding: 0 24rpx;
  border: 1rpx solid #ddd;
  border-radius: 36rpx;
  font-size: 28rpx;
  background: #f5f5f5;
}

.send-btn {
  margin-left: 16rpx;
  width: 72rpx;
  height: 72rpx;
  padding: 0;
  background: #5B8C5A;
  color: #fff;
  border-radius: 50%;
  font-size: 28rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* Prescription card — Chinese medicine aesthetic */
.prescription-card {
  margin-top: 16rpx;
  background: linear-gradient(175deg, #fdf8f0 0%, #faf3e6 40%, #f7edd8 100%);
  border: 2rpx solid #d4c5a0;
  border-radius: 12rpx;
  box-shadow: 0 4rpx 20rpx rgba(139, 105, 20, 0.12), inset 0 0 0 8rpx #fefbf6;
  overflow: hidden;
}

.rx-inner {
  padding: 32rpx 28rpx 24rpx;
  border: 1rpx solid #e8dcc8;
  border-radius: 4rpx;
  margin: 12rpx;
}

.rx-seal-row {
  text-align: center;
  margin-bottom: 8rpx;
}

.rx-seal {
  display: inline-block;
  padding: 16rpx 40rpx;
  background: #8B2500;
  color: #f5e6d0;
  font-size: 32rpx;
  font-weight: 700;
  letter-spacing: 4rpx;
  border: 3rpx double #c4956a;
  box-shadow: 0 2rpx 8rpx rgba(139, 37, 0, 0.25);
  position: relative;
}

.rx-subtitle {
  display: block;
  text-align: center;
  font-size: 22rpx;
  color: #b5a080;
  letter-spacing: 6rpx;
  margin-top: 6rpx;
}

.rx-desc {
  display: block;
  font-size: 24rpx;
  color: #8b7355;
  line-height: 1.8;
  text-align: center;
  margin-top: 16rpx;
  padding: 0 12rpx;
}

.rx-ornament {
  text-align: center;
  font-size: 36rpx;
  color: #c4a882;
  margin: 20rpx 0 4rpx;
  letter-spacing: 12rpx;
}

.rx-products-title {
  text-align: center;
  font-size: 26rpx;
  font-weight: 600;
  color: #6b4f30;
  letter-spacing: 6rpx;
  margin-bottom: 12rpx;
}

.rx-report-btn {
  width: 100%;
  height: 76rpx;
  line-height: 76rpx;
  background: linear-gradient(135deg, #8B4513, #6B3410);
  color: #faf3e6;
  border-radius: 8rpx;
  font-size: 26rpx;
  font-weight: 600;
  letter-spacing: 2rpx;
  margin-top: 20rpx;
  border: none;
}

/* Catalog */
.catalog-container {
  margin-top: 8rpx;
}

.catalog-group {
  margin-bottom: 24rpx;
}

.catalog-header {
  padding: 16rpx 0 8rpx;
}

.catalog-ctype {
  display: inline-block;
  padding: 8rpx 24rpx;
  background: linear-gradient(135deg, #8B6914, #6B4914);
  color: #fff;
  border-radius: 32rpx;
  font-size: 24rpx;
  margin-bottom: 8rpx;
}

.catalog-desc {
  font-size: 24rpx;
  color: #999;
  display: block;
}
